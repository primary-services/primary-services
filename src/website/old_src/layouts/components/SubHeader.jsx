export const SubHeader = ({ children }) => {
	return (
		<header className="sub-header">
			<p>
				Giving voters the tools they need to make informed decisions about the
				candidates in their district
			</p>
		</header>
	);
};
